# Media assets

Drop your captures here so the badges in the root `README.md` resolve:

| File                    | What to capture                                          |
| ----------------------- | -------------------------------------------------------- |
| `demo-drawing.gif`      | Drawing shapes, editing colours, resize / delete.        |
| `demo-collab.gif`       | Two windows side-by-side: live cursors + synced editing. |
| `screenshot-light.png`  | A populated board in light theme.                        |
| `screenshot-dark.png`   | The same board in dark theme.                            |

Tip: record a short clip with your OS screen recorder and convert to GIF
(e.g. with `ffmpeg` or an online converter), keeping it under ~10&nbsp;MB.
